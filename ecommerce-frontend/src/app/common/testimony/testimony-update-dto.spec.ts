import { TestimonyUpdateDto } from './testimony-update-dto';

describe('TestimonyUpdateDto', () => {
  it('should create an instance', () => {
    expect(new TestimonyUpdateDto()).toBeTruthy();
  });
});
