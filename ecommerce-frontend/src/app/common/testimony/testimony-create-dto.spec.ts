import { TestimonyCreateDto } from './testimony-create-dto';

describe('TestimonyCreateDto', () => {
  it('should create an instance', () => {
    expect(new TestimonyCreateDto()).toBeTruthy();
  });
});
